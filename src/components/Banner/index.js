import React from 'react';
import styles from './Banner.module.css';

function Banner() {
    return (
        <section className={styles.banner}>
            {/* Colocar uma imagem ou vídeo de fundo aqui */}
            <h1>Filme</h1>
        </section>
    );
}

export default Banner;